export type UnlockedDreamMetadata = {
  tile: string
  dream: string
  journal: string[]
}

export type DreamMetadata = {
  name: string
  image: string
  tile: string
  description: string
  journal: string[]
}
